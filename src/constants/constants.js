const ALL_CONTSANTS = {
    PAGE_ROUTES: {
        LOGIN: "/",
        SIGNUP: "/signup",
        GYM_HOME: "/dashboard"
    }
}

const CONSTANTS = Object.freeze(ALL_CONTSANTS);
export default CONSTANTS;